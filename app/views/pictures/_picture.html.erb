<div class="photo">
  <%= link_to image_tag(picture.public_filename(:thumb)),
        gallery_picture_url(picture.gallery, picture),
        :style => 'display:block;' %>
  <p><%=h "#{picture.title}" %><br />
  <%= picture.description %></p>
</div>